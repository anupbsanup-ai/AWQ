[build]
  command = "pip install -r requirements.txt"
  publish = "."

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200

[dev]
  command = "uvicorn app.main:app --reload"
  targetPort = 8000
